{
  "name": "beergarden",
  "private": true,
  "author": "Liran Sharir <lsharir@gmail.com>",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm run install-server-dependencies && npm run install-client-dependencies",
    "install-server-dependencies": "cd server && npm install",
    "install-client-dependencies": "cd client && npm install",
    "start": "npm install && npm run build-client && npm run start-server",
    "build-client": "cd client && npm run build",
    "start-server": "cd server && npm run prod"
  }
}
